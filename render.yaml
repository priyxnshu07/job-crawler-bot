services:
  - type: web
    name: job-scout-pro
    env: docker
    # buildCommand and startCommand are ignored for Docker, but we'll remove them to be clean
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: job-scout-db
          property: connectionString
    healthCheckPath: /login
    plan: free

databases:
  - name: job-scout-db
    databaseName: job_scout_db
    user: job_scout_user
    plan: free
